# ==============================================================================
# ENTORNO DE AUTENTICACIÓN CON CLERK
# ==============================================================================
# Estas variables son esenciales para la integración con Clerk.
# Obtén tus claves desde el dashboard de tu aplicación en Clerk:
# https://dashboard.clerk.com/
#
# Copia este archivo a .env.local y rellena los valores.
# NUNCA subas el archivo .env.local a tu repositorio de Git.
# ------------------------------------------------------------------------------

# -- Claves de API de Clerk (Obligatorias) -------------------------------------
# La clave pública se usa en el frontend para identificar tu aplicación.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# La clave secreta se usa en el backend para verificar la autenticidad de las sesiones.
# ¡MANTENER SECRETA! No exponerla nunca en el lado del cliente.
CLERK_SECRET_KEY=sk_test_your_secret_key_here


# -- Configuración de URLs de Autenticación (Obligatorias) --------------------
# Define las rutas personalizadas para las páginas de inicio de sesión y registro.
# Estas rutas deben coincidir con las páginas creadas en `src/app/(auth)/...`
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up


# -- Comportamiento de Redirección (Opcional pero Recomendado) ----------------
# URL a la que se redirigirá al usuario DESPUÉS de iniciar sesión correctamente.
# Por ejemplo, al dashboard principal de la aplicación.
NEXT_PUBLIC_CLERK_SIGN_IN_FORCE_REDIRECT_URL=/dashboard

# URL a la que se redirigirá al usuario DESPUÉS de registrarse correctamente.
# Generalmente es la misma que la de inicio de sesión.
NEXT_PUBLIC_CLERK_SIGN_UP_FORCE_REDIRECT_URL=/dashboard

# URL a la que se redirigirá al usuario DESPUÉS de cerrar sesión.
# Se recomienda redirigir a la página de inicio de sesión.
NEXT_PUBLIC_CLERK_AFTER_SIGN_OUT_URL=/sign-in

# ==============================================================================
# OTRAS VARIABLES DE ENTORNO (Ej: Base de Datos, Servicios Externos)
# ==============================================================================
# En esta sección puedes añadir otras variables que tu aplicación necesite.
# Ejemplo:
# DATABASE_URL="postgresql://user:password@host:port/database"
# NEXT_PUBLIC_API_URL="https://api.example.com"
